package lupin.view

import lupin.BaseFlowSpec

class ViewPortUpdateTest extends BaseFlowSpec {

  /**
    * Use case 1
    *
    * Expose a CRUD interface for some T and a Query interface for returning those values
    */
  "use case 1" should {
    "be abler to return values" in {

    }
  }
}
