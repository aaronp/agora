package agora.exec.client

import agora.BaseExecSpec
import org.scalatest.FunSuite

class UploadClientTest extends BaseExecSpec {
  "UploadClient.upload" should {
    "upload files to UploadRoutes" in {}
  }

}
