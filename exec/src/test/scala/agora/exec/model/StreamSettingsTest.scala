package agora.exec.model

import agora.rest.BaseRoutesSpec

class StreamingSettingsTest extends BaseRoutesSpec {

  "StreamingSettings" should {
    "be able to stream chunked responses" in {
//      val obs = ExchangeObserver()
//      ExchangeHttp(123.asJob.withAwaitMatch(false)) ~> routes(obs) ~> check {
//        val resp = responseAs[SubmitJobResponse]
//        resp.id should not be (null)
//      }
    }
  }
}
